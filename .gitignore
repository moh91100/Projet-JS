# --- Dépendances (Backend & Frontend) ---
node_modules/
package-lock.json # Optionnel : certains préfèrent le garder, d'autres non (si tu utilises yarn, ignore yarn.lock)

# --- Variables d'environnement & Sécurité (TRÈS CRITIQUE) ---
# C'est ici que tu protèges tes clés API (IA), tes secrets JWT et mots de passe DB
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- Build & Production (Frontend React/Vite) ---
dist/
build/
.eslintcache

# --- Logs & Debug ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log
pids/
*.pid
*.seed
*.pid.lock

# --- Docker & Base de données ---
# Si tu as monté un volume local pour que ta DB persiste (ex: ./pgdata:/var/lib/postgresql/data)
# Il ne faut SURTOUT PAS envoyer ta base de données binaire sur GitHub
pgdata/
postgres_data/
.docker/

# --- Cypress (Tests) ---
# On ne veut pas stocker les vidéos et captures d'écran des tests sur le repo
cypress/videos/
cypress/screenshots/

# --- IDE & Système (Fichiers parasites) ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Couverture de code (Optionnel si tu ajoutes des tests unitaires plus tard) ---
coverage/
